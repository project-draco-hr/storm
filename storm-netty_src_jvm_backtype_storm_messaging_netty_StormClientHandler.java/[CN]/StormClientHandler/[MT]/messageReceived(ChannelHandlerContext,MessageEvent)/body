{
  TaskMessage msg=(TaskMessage)event.getMessage();
  if (msg.task() != Util.OK)   LOG.info("failure response:" + msg);
  Channel channel=event.getChannel();
  try {
    sendRequests(channel,client.takeMessages());
  }
 catch (  InterruptedException e) {
    channel.close();
  }
}
