{
  int count=0;
  StringBuilder sb=new StringBuilder();
  for (int i=0; i < n; i++) {
    if (++count > 1) {
      sb.append(", ");
    }
    sb.append(var).append(".").append("get(").append(i).append(")");
  }
  return sb.toString();
}
