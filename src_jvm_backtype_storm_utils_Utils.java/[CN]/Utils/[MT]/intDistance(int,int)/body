{
  if (i2 < i1)   return -1 * intDistance(i2,i1);
  long l1=i1;
  long l2=i2;
  long cand1=l2 - l1;
  long cand2=(long)Integer.MAX_VALUE - l2 + 1 + l1 - Integer.MIN_VALUE;
  if (cand1 < cand2) {
    return (int)cand1;
  }
 else {
    return -1 * (int)cand2;
  }
}
