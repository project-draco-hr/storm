def nextTuple(self):
    sleep((1.0 / 2))
    word = choice(words)
    id = str(uuid4())
    self.pending[id] = word
    log(((('SSS: ' + word) + ' ') + str(ord(word[(-1)]))))
    emit([word], id=id)
