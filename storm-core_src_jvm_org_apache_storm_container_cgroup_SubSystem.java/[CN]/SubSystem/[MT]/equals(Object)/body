{
  boolean ret=false;
  if (object != null && object instanceof SubSystem) {
    ret=((this.type == ((SubSystem)object).getType()) && (this.hierarchyID == ((SubSystem)object).getHierarchyID()));
  }
  return ret;
}
