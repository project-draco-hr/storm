{
  int newValue=size;
  while (true) {
    int oldValue=pacemakerStats.largestHeartbeatSize.get();
    if (newValue > oldValue) {
      if (!pacemakerStats.largestHeartbeatSize.compareAndSet(oldValue,newValue))       continue;
    }
    break;
  }
}
