{
  windowManager=new WindowManager<Integer>(null){
    @Override public void add(    Event<Integer> event){
      eventList.add(event);
    }
  }
;
  waterMarkEventGenerator=new WaterMarkEventGenerator<>(windowManager,50,5,Collections.singleton(streamId("s1")));
}
