{
  inflight.clear();
  tracker.offsets.clear();
  retryList.clear();
  reader.close();
  reader=null;
  try {
    lock.release();
  }
 catch (  IOException e) {
    LOG.error("Unable to delete lock file : " + this.lock.getLockFile(),e);
  }
  lock=null;
}
