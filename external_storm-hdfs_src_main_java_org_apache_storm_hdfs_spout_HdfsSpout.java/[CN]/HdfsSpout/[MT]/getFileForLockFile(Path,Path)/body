{
  String lockFileName=lockFile.getName();
  Path dataFile=new Path(sourceDirPath + lockFileName + inprogress_suffix);
  if (hdfs.exists(dataFile))   return dataFile;
  dataFile=new Path(sourceDirPath + lockFileName);
  if (hdfs.exists(dataFile))   return dataFile;
  return null;
}
