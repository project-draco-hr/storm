{
  long now=System.currentTimeMillis();
  LogEntry entry=new LogEntry(now,componentID,fileOffset);
  String line=entry.toString();
  if (prefixNewLine)   stream.writeBytes(System.lineSeparator() + line);
 else   stream.writeBytes(line);
  stream.flush();
  lastEntry=entry;
}
