{
  Double res=0.0;
  for (  String node : cluster) {
    res+=_availNodes.get(this.NodeHostnameToId(node)).getAvailableMemoryResources() + _availNodes.get(this.NodeHostnameToId(node)).getAvailableCpuResources();
  }
  return res;
}
