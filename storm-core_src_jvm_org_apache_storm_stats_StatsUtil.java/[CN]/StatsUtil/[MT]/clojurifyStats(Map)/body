{
  Map ret=new HashMap();
  for (  Object o : stats.entrySet()) {
    Map.Entry entry=(Map.Entry)o;
    ExecutorInfo executorInfo=(ExecutorInfo)entry.getKey();
    ExecutorStats executorStats=(ExecutorStats)entry.getValue();
    ret.put(Lists.newArrayList(executorInfo.get_task_start(),executorInfo.get_task_end()),clojurifyExecutorStats(executorStats));
  }
  return ret;
}
