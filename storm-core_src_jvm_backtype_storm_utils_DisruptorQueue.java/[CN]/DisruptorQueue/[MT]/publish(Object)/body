{
  Long id=getId();
  ThreadLocalBatcher batcher=_batchers.get(id);
  if (batcher == null) {
    batcher=new ThreadLocalBatcher();
    _batchers.put(id,batcher);
  }
  batcher.add(obj);
  batcher.flush(false);
}
