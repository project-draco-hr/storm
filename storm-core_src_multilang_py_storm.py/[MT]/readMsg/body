def readMsg():
    msg = ''
    while True:
        line = sys.stdin.readline()[0:(-1)]
        if (line == 'end'):
            break
        msg = ((msg + line) + '\n')
    return json_decode(msg[0:(-1)])
