{
  server.channelConnected(e.getChannel());
  if (channel != null) {
    LOG.debug("Replacing channel with new channel: " + channel.toString() + " -> "+ e.getChannel().toString());
  }
  channel=e.getChannel();
  server.channelConnected(channel);
}
