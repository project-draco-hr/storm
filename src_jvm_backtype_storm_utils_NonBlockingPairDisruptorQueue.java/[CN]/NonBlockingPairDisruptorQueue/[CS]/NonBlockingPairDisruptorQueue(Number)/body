{
  _buffer=new RingBuffer<MutableObject>(new DisruptorQueue.ObjectEventFactory(),new SingleThreadedClaimStrategy(bufferSize.intValue()),new BlockingWaitStrategy());
  _consumer=new Sequence(Sequencer.INITIAL_CURSOR_VALUE);
  _buffer.setGatingSequences(_consumer);
  _barrier=_buffer.newBarrier(_consumer);
}
