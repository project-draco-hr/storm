def run(self):
    (conf, context) = initbolt()
    self.initialize(conf, context)
    try:
        while True:
            tup = readTuple()
            self.process(tup)
    except Exception as e:
        log(traceback.format_exc(e))
