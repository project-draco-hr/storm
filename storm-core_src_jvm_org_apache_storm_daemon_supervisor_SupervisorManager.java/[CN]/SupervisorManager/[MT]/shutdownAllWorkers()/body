{
  IWorkerManager workerManager=supervisorData.getWorkerManager();
  SupervisorUtils.shutdownAllWorkers(supervisorData.getConf(),supervisorData.getSupervisorId(),supervisorData.getWorkerThreadPids(),supervisorData.getDeadWorkers(),workerManager);
}
