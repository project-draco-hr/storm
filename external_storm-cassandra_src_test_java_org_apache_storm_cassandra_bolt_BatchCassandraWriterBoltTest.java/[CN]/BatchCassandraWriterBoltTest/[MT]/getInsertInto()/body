{
  return simpleQuery("INSERT INTO weather.temperature(weatherstation_id,event_time,temperature) VALUES (?, ?, ?);").with(field("weatherstation_id"),field("event_time").now(),field("temperature")).build();
}
