{
  Double res=0.0;
  for (  String node : cluster) {
    res+=_nodes.getNodeById(this.NodeHostnameToId(node)).getAvailableMemoryResources() + _nodes.getNodeById(this.NodeHostnameToId(node)).getAvailableCpuResources();
  }
  return res;
}
